{
  "name": "context-editor-tab",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that opens a persistent code/text editor tab for AI tab-context tools (e.g. Gemini in Chrome) to read.",
  "author": "Aman Ranjan Verma",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build:extension && npm run build:editor",
    "build:extension": "vite build --config vite.extension.config.ts",
    "build:editor": "vite build --config vite.editor.config.ts",
    "dev:editor": "vite --config vite.editor.config.ts",
    "preview:editor": "npm run build:editor && vite preview --config vite.editor.config.ts",
    "package": "npm run build:extension && node scripts/package-extension.cjs",
    "icons": "node scripts/make-icons.cjs"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.31",
    "@types/chrome": "^0.0.279",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "dompurify": "^3.4.12",
    "marked": "^18.0.6",
    "mermaid": "^11.16.0",
    "monaco-editor": "^0.50.0"
  }
}
